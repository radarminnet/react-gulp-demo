/**
 * @jsx React.DOM
 */
var React = require("react");

var AppContainer = React.createClass({
    render: function() {
        return <div>
            <h1>REACT IS LIVE!</h1>
        </div>;
    }
});

module.exports = AppContainer;
